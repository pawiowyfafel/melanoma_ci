{
  "name": "dermascan-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --ci"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/*.test.js"],
    "reporters": ["default", "jest-junit"]
  }
}
